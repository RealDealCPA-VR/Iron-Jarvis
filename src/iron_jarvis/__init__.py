"""Iron Jarvis — a local-first AI operating system.

This package is the Phases 0–3 vertical slice: daemon + event bus + persistence
(Phase 0), tool registry + permission engine (Phase 1), provider manager + model
router + MockLLM (Phase 2), and a single-agent runtime loop (Phase 3).
"""

__version__ = "0.1.0"
